# learning-things

Personal notes on programming concepts, built up one question at a time. Notes are grouped into category folders; each category folder has its own `images/` subfolder for diagrams.

## DSA

- [dsa/dsa-prep-plan.md](dsa/dsa-prep-plan.md) — a 4-phase, 2–3 month DSA interview prep plan for someone who's covered most topics but lacks confidence applying them: overview of all 4 phases (Phase 1 detailed separately in phase-1-diagnostic.md), pattern-notebook drilling with interleaved practice, timed applied practice on curated lists, mock-heavy consolidation, plus common prep mistakes vs. what actually works.
- [dsa/phase-1-diagnostic-tracker.csv](dsa/phase-1-diagnostic-tracker.csv) — ready-to-use tracking sheet for the Phase 1 diagnostic, pre-filled with all 36 problems (topic, problem, difficulty, time limit) mapped to their scheduled day; open in Excel/Sheets and fill in the remaining columns while practicing.
- [dsa/phase-1-diagnostic.md](dsa/phase-1-diagnostic.md) — the full Phase 1 diagnostic: rules for each timed problem (25 min, closed-book, confidence 1–5 scoring), a 12-topic list with 3 representative problems each, a day-by-day 2-week schedule, the tracking sheet columns, and how to turn results into a Phase 2 weak-topic list.

## Elasticsearch

- [elasticsearch/elasticsearch-from-scratch.md](elasticsearch/elasticsearch-from-scratch.md) — Elasticsearch from the ground up in the order you'd actually learn it: the index/document/`_id`/`_source`/mapping vocabulary and the SQL mental model (and the two places it leaks — ranked lists instead of sets, and no joins), field types and the `text` vs `keyword` split (why one is analyzed and one isn't, why `term` on a `text` field silently returns nothing, and why `.keyword` multi-fields exist), the basic queries (`match`, `term`, `terms`, `range`), the full `bool` structure with nested bools and AND/OR/NOT translation, `must` vs `filter` as eligibility-vs-ranking (and why business constraints belong in `filter`), `should` as boost-vs-implicit-OR depending on its neighbours, `_score` and how an explicit `sort` silently discards relevance, BM25 conceptually (TF saturation, IDF, field-length normalisation, and why keyword stuffing doesn't win), `multi_match` with `^` field boosting and `best_fields`, fuzziness (`AUTO` edit distances, the recall/precision trade-off, and how to apply it per-field), `operator: and` vs `minimum_should_match` (including `"2<75%"`), `match_phrase` and `slop`, boosting, the analyzer pipeline and why `Running` doesn't match `run`, `nested` mapping with the concrete cross-object false-match bug it prevents, sorting, pagination and the 10,000-result window (plus `search_after`), `_source` filtering, aggregations as `GROUP BY` with metric sub-aggregations, the CRUD APIs and `_id` vs a business `productId`, partial update vs full replacement, the Bulk API's NDJSON format and silent partial failures, `_update_by_query`, a 10-step production debugging checklist with symptom-to-cause table, a complete worked product-search example (mapping → documents → Query DSL → scoring walkthrough → SQL equivalent), Kotlin client examples for building the query and bulk-indexing, decision checklists for every fork with real-life examples per option, interview Q&A, and a one-page cheat sheet.

## General

- [general/dev-random-and-entropy.md](general/dev-random-and-entropy.md) — `/dev/random` as a character *device* rather than a file, why software can't manufacture unpredictability so the kernel harvests it from hardware (interrupt timings, disk/network jitter, `RDSEED`), and the step people miss: that noise **seeds a ChaCha20 CSPRNG once** and reads are served from the cipher, not from raw noise; why the classic "use `/dev/random` for long-lived keys" advice is **obsolete** — the entropy-depletion model it rests on doesn't apply to a seeded CSPRNG, and since Linux 5.6 `/dev/random` blocks only until initialisation; that the one genuine danger is reading *before* seeding (the 2012 *Mining Your Ps and Qs* factorable-key study), making `getrandom(2)` the modern correct API; macOS/BSD where both devices are identical and never block; the JVM mapping where `new SecureRandom()` reads urandom and never blocks while `getInstanceStrong()` reads `/dev/random` and hangs startup on fresh VMs (plus what the odd `file:/dev/./urandom` flag actually does and why containers are *not* separately entropy-starved); the Kotlin trap that `kotlin.random.Random` is not cryptographically secure and sits one import from `SecureRandom`; why alerting on `entropy_avail` is obsolete folklore; a decision checklist for secure vs plain PRNG with real-life examples per option; and common mistakes.
- [general/multi-account-github-setup.md](general/multi-account-github-setup.md) — managing multiple GitHub accounts on one machine: one SSH key per account with an `~/.ssh/config` alias per account, and git identity (name/email) scoped by repo directory via `includeIf "gitdir:..."` in `~/.gitconfig` — plus how the two systems combine and don't have to match by construction.
- [general/passkeys.md](general/passkeys.md) — what problem passkeys solve (eliminating the shared-secret weaknesses of passwords), how WebAuthn/FIDO2 registration and login work, why they resist phishing, and synced vs. device-bound passkeys.
- [general/qr-codes.md](general/qr-codes.md) — how a QR code encodes data as a grid, its finder/alignment/timing patterns, and Reed-Solomon error correction.
- [general/solid-principles.md](general/solid-principles.md) — the five principles framed as one idea (isolating change, so a modification touches one place instead of ten — and therefore worthless for code that never changes): **SRP** as one *actor* per module rather than the misreading "one thing per class" that produces a blizzard of tiny classes, with an OrderService whose place/renderPdf/emailInvoice methods answer to finance, design and marketing; **OCP** with a payment-fee `when` block versus a registry of `PaymentProcessor` implementations, plus the Kotlin twist that a `sealed` hierarchy is *deliberately closed* and better when variants are fixed while operations grow (the expression problem), and why speculative one-implementation interfaces are pure cost; **LSP** as a behavioural contract (don't strengthen preconditions, weaken postconditions, or add surprise exceptions) shown via an audited repository that demands an authenticated user its parent never required and so breaks a batch job, plus the JDK's own violation in `Arrays.asList().add()` and how Kotlin's `List`/`MutableList` split is a language-level fix; **ISP** with a fat payment gateway forcing a payout-only provider to throw on four methods — showing how an ISP violation *causes* an LSP violation — versus role interfaces; **DIP** where the actual inversion is *interface ownership* (the domain declares the port, the adapter implements it inward — ports and adapters), and why DIP is a direction while dependency injection is only a mechanism; a "when SOLID misleads" section on over-abstraction and which principles Kotlin's top-level functions, data classes, sealed types and function types make unnecessary; decision checklists for extracting an interface, choosing interface vs sealed, and whether an SRP split is worth it; and common mistakes.
- [general/totp.md](general/totp.md) — how TOTP generates rotating 6-digit codes from a shared secret + time step (HOTP + HMAC-SHA1 under the hood), and why it's not phishing-resistant the way passkeys are.
- [general/zip-file-format.md](general/zip-file-format.md) — ZIP compression (LZ77 + Huffman/DEFLATE) and the container format's per-file entries plus central directory.

## Interview Prep

- [interview-prep/english-speaking-practice.md](interview-prep/english-speaking-practice.md) — improving spoken English fluency when comprehension is already strong (the receptive vs. productive skill gap): thinking in English instead of translating, shadowing, forced daily output, recording yourself, targeted pronunciation drilling, a progressive exposure ladder from self-talk to real interviews, a concrete daily/weekly routine with named tools (BBC 6 Minute English, Tandem/HelloTalk, ELSA Speak), and common mistakes vs. what actually works.
- [interview-prep/recruiter-and-interviewer-responses.md](interview-prep/recruiter-and-interviewer-responses.md) — a running collection of common recruiter/interviewer prompts contrasting weak vs. high-value candidate responses (stating a scheduling preference while staying flexible; handling "do you have other competing offers?" without over-disclosing or losing leverage), with the signal each sends and the general principle behind it.
- [interview-prep/remote-and-germany-job-search.md](interview-prep/remote-and-germany-job-search.md) — where to search for remote/German developer roles (global remote boards vs. Germany/English-speaking-specific ones), EU Blue Card visa thresholds and the IT shortage-occupation/no-degree path, gross-vs-net and remote-band comp differences, German interview-process/notice-period expectations, whether German is actually required, and weak-vs-high-value answers for the visa/language/relocation questions.
- [interview-prep/salary-negotiation-strategy.md](interview-prep/salary-negotiation-strategy.md) — how to arrive at a CTC ask for an India job switch: why to anchor off current CTC rather than in-hand pay, hike-percentage benchmarks by switch type (services-to-services, services-to-product, specialized stacks), a worked example, where to actually check compensation data filtered by experience and skills (Naukri Salary Predictor, PayScale, AmbitionBox, Levels.fyi, Glassdoor, Blind/Grapevine), and why generic "average salary" blog aggregators are unreliable.

## Java

- [java/garbage-collection-vs-manual-memory-management.md](java/garbage-collection-vs-manual-memory-management.md) — why Java has no `delete` and relies on a tracing garbage collector (GC roots, reachability) while C++ uses deterministic RAII/smart pointers, the design philosophy behind that split (zero-overhead vs. safety), reference-cycle handling, why the JVM **relocates live objects** so an address is never stable (compaction and bump-pointer allocation, which collectors move things, forwarding pointers and card tables, ZGC's coloured pointers and Shenandoah's forwarding word for concurrent relocation, and why that means no pointer arithmetic, `identityHashCode` isn't an address, and JNI needs handles or pinning), a decision checklist for GC vs. manual/RAII memory management, and common Java memory-leak patterns (static caches, unremoved listeners/ThreadLocals, non-static inner classes, classloader leaks).
- [java/hashmap.md](java/hashmap.md) — how a HashMap achieves near-O(1) lookup via bucket arrays and a hash function, how collisions are chained (and tree-ified in Java 8+), resizing/load factor, the hashCode/equals contract, and why the default capacity is 16 and always doubles (power-of-two bitmask indexing, and the single-bit resize shortcut doubling enables).
- [java/java-concurrency.md](java/java-concurrency.md) — Java concurrency as one story: the three failure modes (atomicity, visibility, ordering) and the JMM's happens-before rules, what `synchronized` actually guarantees (mutual exclusion *and* visibility, reentrant, never leaks the lock) with a full pros/cons table, then the tool that fixed each weakness — `volatile` for visibility only, atomics/`LongAdder` for lock-free CAS, `ReentrantLock`/`ReadWriteLock`/`StampedLock` for timeouts, interruptibility, fairness and multiple conditions, concurrent collections instead of one big lock, executors and `CompletableFuture` instead of hand-managed threads, and virtual threads (Java 21+, with the JDK 24 pinning fix) — plus cooperative interruption (why entering `synchronized` ignores `interrupt()` while `lockInterruptibly()` doesn't, what responds to an interrupt and what doesn't, and the `InterruptedException` contract that makes `shutdownNow()` work), synchronizers (`CountDownLatch`, `Semaphore`, …), immutability/confinement as the best option of all, classic failure modes (deadlock, check-then-act, false sharing, `ThreadLocal` leaks, blocking the common pool), and a decision checklist for which tool fits which problem.
- [java/java-memory-management.md](java/java-memory-management.md) — the JVM's runtime data areas (per-thread stack/PC register vs. shared heap/method area), where local variables and instance/static fields actually live, escape analysis, the heap's generational structure (Eden/Survivor/Old, PermGen vs. Metaspace), compressed oops (why a 64-bit JVM stores 4-byte references as scaled offsets, the encode/decode and zero-based mode, the 32 GB cliff where they silently switch off so a bigger heap holds less data, object-header sizes, compressed class pointers, and why ZGC opts out), and the string pool/interning.
- [java/java-memory-model-happens-before.md](java/java-memory-model-happens-before.md) — the Java Memory Model in depth: why it must exist (compiler, JIT, CPU and caches all reorder, and x86 vs ARM differ), the reordering example where both threads legally read 0, visibility vs ordering as two distinct failures (register caching/store buffers/per-core caches vs compiler and CPU reordering, the cache-coherence trap, and why one edge fixes both), what happens-before actually means (a visibility/ordering guarantee, *not* a statement about time — a transitive partial order), the closed list of rules that create an edge (program order, monitor release→acquire, volatile write→read, `start()`, `join()`, interruption, class init) plus `final`-field freeze, the piggybacking pattern every `java.util.concurrent` handoff uses, data race vs race condition and the data-race-free ⟹ sequentially-consistent promise, the hoisted-flag bug, non-volatile `long`/`double` tearing, the four safe-publication idioms and why double-checked locking needs `volatile`, common misconceptions, a checklist for picking the edge you need, and why testing can't prove a race absent (jcstress, run on ARM).
- [java/java-program-execution.md](java/java-program-execution.md) — what happens when you run `java Demo`: JVM startup, the classloader delegation hierarchy and lazy loading, how a ClassLoader actually works (loadClass/findClass/defineClass, custom classloaders, (name, loader) type identity, parent-first vs. child-first), what the classpath actually is (`-cp`, manifest `Class-Path`, directory vs. JAR entries) and the module path alternative, linking (verification/preparation/resolution), static initialization, finding and invoking `main`, the interpreter-then-JIT (C1/C2 tiered compilation) execution engine, and JVM shutdown.
- [java/java-serialization.md](java/java-serialization.md) — turning an object graph into bytes and back: a flat-pack-furniture analogy, a deep look at why a pointer can't be written to disk (object header/klass pointer/reference fields, per-process address spaces and ASLR, the GC relocating objects even within one JVM, platform-specific layout) and how serialization replaces addresses with stream **handles** — sequential IDs from `0x7E0000` held in an identity map, with repeats written as a 5-byte `TC_REFERENCE` back-reference (hence the leak in a long-lived stream and the classic bug where re-writing a mutated object transmits only the stale handle) — converting pointer identity into positional identity, which is what preserves sharing and terminates cycles, and why `transient` and class re-resolution work the way they do, `Serializable` as a marker interface and what actually goes into the stream (class name, `serialVersionUID`, non-static non-transient fields, handles for shared/cyclic refs), `transient`, why `serialVersionUID` must be declared explicitly and which class changes are compatible, why classes override the default form (it writes representation rather than content, `ObjectOutputStream`'s depth-first recursion overflows the stack on linked structures, private field names become a permanent public contract, and the skipped constructor means `readObject` is another public constructor needing validation and defensive copies), exactly how deserialization builds an object without running your constructor, field initializers or instance blocks (`ReflectionFactory`, the nearest non-serializable superclass's no-arg constructor, direct field writes even to `final` fields) and the v1/v2 trap where a new field arrives `null` instead of its initializer value, how field order is determined (superclass blocks first, then primitives before references, alphabetical within each group — because `getDeclaredFields()` order is unspecified — while reading matches by name and type rather than position, and custom `writeObject` data becomes a positional protocol you must mirror exactly or read by name via `GetField`), the customization hooks (`writeObject`/`readObject`, `readResolve`, `writeReplace`, `serialPersistentFields`, `Externalizable`, the serialization-proxy pattern, and records deserializing through their canonical constructor), why it's discouraged (gadget-chain RCE on untrusted input and `ObjectInputFilter`, broken encapsulation, constructor bypass, Java-only, bulky), what to use instead (JSON, protobuf/Avro, Kryo) with a comparison table, a detailed contrast with Jackson `ObjectMapper` JSON (encodes the *data* vs the *object*, constructor-based vs constructor-bypassing reconstruction, tree vs true object graph with shared refs and cycles, type fidelity, `transient` vs `@JsonIgnore`, and why known-target-type parsing is what makes it safer), how the JDK collections serialize (`ArrayList`/`LinkedList`/`HashMap` all mark their internals `transient` and write logical state instead — plus lost capacity, non-serializable `subList()` views, and `List.of`'s proxy), where you still meet it (session replication, RMI, caches, serializable lambdas), a gotchas table, and a decision checklist for how an object should cross a boundary.

## Kotlin

- [kotlin/kotlin-contracts.md](kotlin/kotlin-contracts.md) — how Kotlin contracts (`returns(...) implies`, `callsInPlace`) let functions like `require()`, `isNullOrEmpty()`, and `run`/`let`/`apply` enable smart casts and definite-assignment checks the compiler couldn't otherwise infer, and why an incorrect contract is unsound (declared, not verified).

## Spring Boot

- [spring-boot/http-connections-and-tomcat-threading.md](spring-boot/http-connections-and-tomcat-threading.md) — the layers below "Client → Tomcat": why TCP/TLS handshakes are paid per *connection* (keep-alive) not per request, HTTP/1.1 head-of-line blocking vs HTTP/2 streams and frame multiplexing with a decision checklist for which to use and where to terminate it, Tomcat's acceptor/poller/worker thread split and how `epoll` lets a couple of poller threads watch thousands of sockets, why idle connections cost 0 worker threads, why overload latency is mostly queue wait, and how a reverse proxy fans thousands of client connections into a small backend pool.
- [spring-boot/jpa-hibernate-spring-data-stack.md](spring-boot/jpa-hibernate-spring-data-stack.md) — how Spring Data JPA, JPA, Hibernate, JDBC, and MySQL relate as layers (spec vs. implementation vs. abstraction), a restaurant analogy per layer, the actual call chain behind `repository.save()`, and why each layer is independently swappable.
- [spring-boot/spring-data-jpa-derived-query-methods.md](spring-boot/spring-data-jpa-derived-query-methods.md) — how Spring Data JPA (not Hibernate) parses a repository method name like `findByEmailAndStatusOrderByCreatedAtDesc` into JPQL step by step, the naming grammar and keyword reference table (`And`, `Between`, `Like`, `IgnoreCase`, `OrderBy`, etc.), a fast-food-counter analogy, return-type conventions, and when to switch to `@Query` instead.
- [spring-boot/spring-execution-contexts-and-hooks.md](spring-boot/spring-execution-contexts-and-hooks.md) — hook points beyond a single request's path: bean lifecycle callbacks (@PostConstruct/@PreDestroy, BeanPostProcessor), async/deferred execution (@Async, @Scheduled, message queues), event publishing (ApplicationEventPublisher, @TransactionalEventListener), transaction-boundary hooks, ambient context holders (RequestContextHolder in detail, SecurityContextHolder, MDC), context-scoped beans, a decision checklist for choosing between them, how uncaught exceptions are handled (or silently swallowed) in each one, and the centralized-handling mechanism that plays @ExceptionHandler's role per context (AsyncUncaughtExceptionHandler, ErrorHandler, Kafka's CommonErrorHandler, etc.).
- [spring-boot/spring-request-lifecycle.md](spring-boot/spring-request-lifecycle.md) — how an HTTP request flows through Tomcat, filters, DispatcherServlet, RequestBodyAdvice, interceptors, the controller, AOP proxies, service/repository layers, and back out via message converters and ResponseBodyAdvice, plus the exception-handling path (HandlerExceptionResolver, @ExceptionHandler resolution order) and supporting beans (HandlerAdapter, WebMvcConfigurer, Validator, ConversionService, ObjectMapper, CORS, async support).
- [spring-boot/sql-injection-protection.md](spring-boot/sql-injection-protection.md) — why Spring Boot has no dedicated anti-SQL-injection feature: protection comes from parameterized queries/prepared statements, which Spring Data JPA and JdbcTemplate generate automatically when used as intended (derived methods, `:name`/`?` placeholders, Criteria API); what a prepared statement actually is (raw JDBC prepare/bind/execute lifecycle, batch reuse, its distinct security vs. performance benefits); plus safe-vs-unsafe pattern tables (native queries, string concatenation, unbindable identifiers like column names).

## System Design

- [system-design/async-transaction-confirmation.md](system-design/async-transaction-confirmation.md) — how a mobile app learns whether an async, event-driven transaction (e.g. order/payment) succeeded or failed: polling, long polling, WebSocket/SSE push, and mobile push notifications.
- [system-design/blobs-and-large-object-storage.md](system-design/blobs-and-large-object-storage.md) — what a BLOB actually is (bytes the database stores but never interprets — no charset, no structure, nothing indexable inside) vs CLOB/TEXT, the three other things "blob" means (Azure Blob Storage, the browser `Blob`, Git blobs), how MySQL/Postgres/Oracle/SQL Server really store large values off-row (overflow pages, TOAST, LOB segments) and why `SELECT *` gets expensive, reading/writing them from JDBC and JPA (`@Lob`, why `byte[]` risks OOM and why a `Blob` locator dies with its transaction), why storing a serialized Java object in a BLOB is a long-term trap and `JSON`/`JSONB` is usually the answer, and a decision checklist for in-database bytes vs object storage with the metadata-row + pre-signed-URL + two-phase-upload default.
- [system-design/caching-fundamentals.md](system-design/caching-fundamentals.md) — the core caching mental model: where caching sits in a request path (browser → CDN → reverse proxy → app cache → distributed cache → DB), the read/write patterns (cache-aside, read-through, write-through, write-behind, write-around) with a decision checklist, which pattern each Spring `@Cacheable`/`@CachePut`/`@CacheEvict` annotation actually implements plus runnable Kotlin examples of all five patterns with Spring's caching library, eviction policies (LRU/LFU/FIFO/TTL), invalidation strategies, and how Hibernate's own entity caches fit the same model.
- [system-design/cap-theorem.md](system-design/cap-theorem.md) — CAP theorem: consistency vs availability vs partition tolerance, what a network partition actually is, a decision checklist for CP vs AP with worked examples (bank ledger, leader election, shopping cart, like counts), and example systems for each. Covers **eventual consistency** as the AP branch's actual model (how little "eventually, if writes stop" promises, the ladder from linearizable through causal and session guarantees down to eventual, causal as the strongest model that stays available under partition, why convergence needs an explicit conflict-resolution rule, and why it shows up just as often from async replication on a healthy network as from partitions), plus the three unrelated things called "C" (ACID's, CAP's, and eventual). Also settles **what CAP actually applies to** — replication, not sharding, since the dilemma needs the same key on two reachable nodes, whereas a partition in a sharded-only system just makes a slice of the keyspace unreachable (partial unavailability, not inconsistency); that real clusters shard *and* replicate so the choice is made per replica group, making availability per-key rather than global; that sharding's own correctness problem is cross-shard atomicity (the A of ACID, not of CAP); and that the shard routing map is itself replicated state where the deliberate CP choice usually lives. Also explains why **P is the odd one out** — C and A describe your system's behaviour while P describes what the network does to you, so CAP is a conditional ("if a partition, give up C or A") rather than a menu; that both CP and AP systems *are* partition tolerant and merely tolerate it differently; and why "CA" is a category error whose only honest instance is a single node. Also covers **PACELC** — what governs the 99.9% of the time when there is *no* partition: consistency vs latency, why coordination costs a network round trip (with real RTT numbers), the four PACELC classes (PC/EC, PA/EL, PA/EC, PC/EL) with example systems, why the E branch matters more day-to-day than the P branch, why a partition is operationally just "didn't answer within timeout T" so your timeout config is where the CAP choice actually gets made, the two standard misreadings (CAP's C is linearizability not ACID's C, and CAP's A is absolute so "CP" doesn't mean "often down"), and an EL-vs-EC decision checklist with real-life examples per option.
- [system-design/choosing-sql-vs-nosql.md](system-design/choosing-sql-vs-nosql.md) — decision checklist for picking SQL vs NoSQL for a given problem, with an e-commerce example.
- [system-design/consistent-hashing.md](system-design/consistent-hashing.md) — why `hash(key) mod N` routing remaps ~80% of keys when you add one server to four (and why that's worst exactly when you're scaling up — an invalidated cache tier becomes a thundering herd, a database becomes a full data migration under load), the four properties you actually want from a key-to-node mapping, the ring itself (hash keys and nodes into one circular space, key belongs to the first node clockwise) with a linear walk-through of tokens, ownership and the wrap-around case, why adding or removing a node moves only `~1/N` of keys and only from one neighbour, a `TreeMap`-based Kotlin implementation where `ceilingEntry() ?: firstEntry()` is the single line that makes it a ring, virtual nodes as mandatory rather than optional (uneven random arcs, a dead node dumping its entire load on one successor and cascading, and heterogeneous machine capacity — plus real token counts from ketama's 160 and Cassandra's move from 256 to 16), what consistent hashing does *not* solve (it balances keys not load so hot keys still cook one node, rack/AZ-unaware replica walks, no range queries, still needs a membership source of truth), replication as a clockwise walk to the next R *distinct physical* nodes and the dedup bug that puts three "replicas" on one machine, the alternatives with their real trade-offs (fixed partition count, rendezvous/HRW, jump consistent hash, Maglev) and the honest recommendation that databases usually want fixed partitions instead — including that Redis Cluster and Kafka do *not* use consistent hashing — where it genuinely is used, decision checklists for technique and vnode count with real-life examples per option, and common mistakes.
- [system-design/database-sharding-partitioning-replication.md](system-design/database-sharding-partitioning-replication.md) — scaling a database past one machine: naming the actual bottleneck first (reads vs writes vs dataset size vs availability vs latency vs row width) and why replication scales reads while only sharding scales writes and storage, replication in depth (single-leader with sync/async/semi-sync and the durability-vs-latency dial, the three named replication-lag anomalies — read-your-writes, monotonic reads, consistent prefix reads — each with its fix, failover and why automatic failover causes lost writes and split brain, how the data actually ships as statement/WAL/logical, application-side **read/write splitting** — the three places to implement it (app-level routing DataSource, proxy, provider endpoint), a Spring Boot/Kotlin `AbstractRoutingDataSource` keyed on `@Transactional(readOnly = true)`, what must never be routed to a replica, lag-aware routing that falls back to the primary, and purpose-built analytics/delayed/cascading replicas — multi-leader with its conflict-resolution menu including why last-write-wins is silent data loss, and leaderless quorums with `W + R > N`, read repair and hinted handoff), the terminology settled (vertical vs horizontal partitioning vs sharding, and why native single-instance table partitioning is a reversible afternoon's work while sharding is an architectural commitment), sharding in depth (why the shard key *is* the architecture and which query you're choosing to make fast, range vs hash with the sequential-key hot-shard failure and the compound-key compromise, the six named techniques — range/dynamic, hash/algorithmic, consistent hashing, directory-lookup, entity-group and geo — with a comparison table and the layered combination large SaaS products actually run, the celebrity hot-key problem hashing can't fix (why determinism makes it unfixable by hashing, key salting with the commutative-merge requirement and the atomic-conditional-update it destroys, N-buckets-is-not-N-nodes sizing traps, and why local aggregation usually beats salting), why `hash(key) mod nodeCount` moves ~80% of the data and the three real fixes — fixed logical partition count, consistent hashing, dynamic splitting, routing tiers, local vs global secondary indexes, and the honest list of what sharding breaks: joins, cross-shard ACID, global uniqueness, auto-increment IDs, aggregates and deep pagination), a fully worked example sharding a 1.2 TB order service end to end — measuring the query mix first, rejecting `order_id`/`created_at`/`region` and why, hash-sharding on `customer_id` into 1024 fixed logical partitions, co-locating the entity group so writes stay single-shard ACID, encoding the partition into the order ID to take 93% of traffic single-shard, and pushing dashboards to scatter-gather and analytics to CDC — how shards and replicas combine, an 8-step "try this before sharding" ladder, decision checklists for every fork with real-life examples per option, a real-systems reference table, and common mistakes.
- [system-design/forward-proxy-reverse-proxy-vpn.md](system-design/forward-proxy-reverse-proxy-vpn.md) — the three kinds of middle-man separated by **whose agent they are**: a forward proxy acts for the client and hides it from the server, a reverse proxy acts for the server and hides the backends from the client (the same nginx binary doing both, so topology and config decide the role), while a **VPN isn't a proxy at all** — it operates at Layer 3 and relocates the whole machine onto another network, covering every app and protocol rather than the ones configured to use it; forward proxies in practice (egress control, filtering, caching, package mirrors, explicit vs transparent interception, and the `CONNECT` mechanic that leaves the proxy seeing hostname and SNI but not content unless it MITMs with a corporate root CA — which is why build tools fail TLS when the CA is in the OS truststore but not the JVM's, and why `HTTP_PROXY` doesn't reach the JVM at all); reverse proxies and why client-IP handling behind one becomes a security decision rather than a detail; VPN types (remote-access vs site-to-site, full vs split tunnel, WireGuard/IPsec/OpenVPN) and an honest account of what a VPN does *not* do — it relocates trust to the provider rather than granting anonymity, adds nothing over HTTPS for content, and hands out *network* access instead of application authorisation; how Zero Trust identity-aware proxies (BeyondCorp, Cloudflare Access, Tailscale) replace the VPN with a reverse proxy, converging all three topics; a side-by-side comparison table, decision checklists for which one a problem needs and whether a VPN is still required, and common mistakes.
- [system-design/http-headers.md](system-design/http-headers.md) — the common request and response headers and what each actually does, grouped by job: routing and identity (`Host` as what makes virtual hosting work and becomes `:authority` in HTTP/2, the permanently-misspelled `Referer` vs the correctly-spelled `Referrer-Policy`, `Origin`); content negotiation with `q=` values and why whatever you negotiate on must appear in `Vary`; body description including the `Content-Length` + `Transfer-Encoding` combination that enables **HTTP request smuggling**; auth and state; the conditional-request family where `If-None-Match` saves bandwidth and **`If-Match` gives you optimistic concurrency with no locks**; hop-by-hop connection headers (`Connection: Upgrade`) that proxies must strip and HTTP/2 forbids outright; proxy headers (`X-Forwarded-For`/`-Proto`/`-Host`, RFC 7239 `Forwarded`) and why trusting them from any source breaks rate limiting and audit logs — plus the `X-Forwarded-Proto` omission that causes redirect loops; tracing (`traceparent`, `Idempotency-Key`); unforgeable `Sec-Fetch-*` metadata as a CSRF signal; response-side caching, cookie attributes, CORS (a *browser* policy, not server security), security headers and `RateLimit-*`; the RFC 6648 deprecation of the `X-` prefix; what HTTP/2 changed (lowercase names, pseudo-headers, HPACK); header size limits as a "works locally, fails behind the gateway" bug; decision checklists for ETag-vs-Last-Modified, Cookie-vs-Bearer and X-Forwarded-vs-Forwarded; and common mistakes.
- [system-design/http-protocol-versions.md](system-design/http-protocol-versions.md) — how HTTP/1.0, 1.1, 2 (and 3) differ: same semantics, different wire behaviour — 1.0's one-request-per-connection handshake tax and missing `Host` header, 1.1's keep-alive, virtual hosting, chunked streaming and `Cache-Control`/`ETag` caching (plus why its pipelining was specified-but-unusable and browsers opened ~6 connections instead), HTTP/2's binary framing, streams/multiplexing, HPACK, prioritization and withdrawn server push, a delivery-van analogy, why sharding/concatenation/sprites became anti-patterns, the TCP-level head-of-line blocking HTTP/2 left for HTTP/3 to fix, a full comparison table, and a which-version-where checklist.
- [system-design/idempotency.md](system-design/idempotency.md) — what idempotency means, where it shows up (HTTP methods, idempotency keys, at-least-once message delivery), the concrete mechanisms used to achieve it (dedup stores, conditional writes, unique constraints, inbox pattern), and client-generated vs server-issued keys with the "unused/consumed/unknown" three-state check needed to avoid re-processing or wrongly discarding retries.
- [system-design/latency-percentiles.md](system-design/latency-percentiles.md) — what p50/p90/p95/p99 actually mean (the value N% of requests came in under) and the nearest-rank arithmetic behind them, worked by hand — including that the rank scales with sample count rather than the label's digits (at n=1000, p99 is the **990th** item, not the 99th) and that below ~100 samples p99 silently degenerates into "the slowest request in the window"; why the mean lies on a right-skewed distribution (a worked 1000-request example where the mean is 230 ms, p50 is 20 ms, p99 is 8 s, and *no request took 230 ms*); why **percentiles can't be averaged** — a position in a distribution doesn't add, so per-instance p99s must be merged as histograms (why Prometheus `histogram_quantile` is right and `summary` can't aggregate) and why bucket edges must sit near your SLO or you're alerting on bucket layout; **tail amplification** — 100 fan-out calls each at p99=1s means ~63% of user requests are slow, so the backend's p99 becomes the user's median, with hedged/tied requests and fan-out reduction as fixes; where tails come from (queueing non-linearity, GC pauses, cold caches, retries, contention, noisy neighbours, head-of-line blocking, data skew) and why a flat p50 with a rising p99 means contention rather than uniformly slower code; **coordinated omission** and why closed-loop load generators understate exactly the tail you're measuring; SLI/SLO/SLA and error budgets; Micrometer/Kotlin instrumentation; a decision checklist for which percentile an SLO should use with real-life examples per option; and common mistakes.
- [system-design/load-balancing-algorithms.md](system-design/load-balancing-algorithms.md) — how a load balancer picks a backend, framed as one trade-off (better decisions need backend state, and stale state is worse than none): round robin and its three real failure modes (variable request cost, long-lived HTTP/2 and gRPC connections where connection fairness says nothing about request fairness, and heterogeneous or just-restarted backends); weighted round robin as static config describing a dynamic world; random as an honest baseline and its `log n / log log n` worst case; **least connections** as the best general-purpose default because in-flight count is a live measurement of business, plus its empty-node stampede and multiple-balancer blind spots; least response time / peak EWMA and why a latency feedback loop oscillates; **power of two choices** and the result that two random samples give an exponential improvement over one while a third adds almost nothing — hence the Envoy/mesh default; hash-based policies (source-IP, ring hash, Maglev, bounded-load) where the purpose is affinity rather than balance and one hot key still cooks one backend; zone-aware routing for cross-AZ latency and billing, and the hard-pin hot spot it can create; the supporting machinery that matters more than the algorithm (active vs outlier health checks, slow start, connection draining, circuit breaking, retry budgets); real-world defaults across nginx, HAProxy, Envoy, ALB/NLB, gRPC and Linkerd; decision checklists with real-life examples per option; and common mistakes.
- [system-design/load-balancing-l4-vs-l7.md](system-design/load-balancing-l4-vs-l7.md) — what a load balancer buys you (scale, availability, zero-downtime deploys, one stable address) and the L4/L7 split: L4 balances *connections* on IP+port with the payload sealed (protocol-agnostic, fastest, TLS passthrough, but no path routing, no retries, and it pins HTTP/2/gRPC to one backend) vs L7 balancing *requests* after parsing them (path/host/header routing, retries, canary, cookie stickiness, WAF — at the cost of CPU and holding your TLS keys), a sorting-facility vs receptionist analogy, balancing algorithms (round robin, least-connections/least-request, EWMA, P2C, consistent hashing), L4 vs L7 health checks and connection draining, sticky sessions, HAProxy `mode tcp` vs `mode http` config, why real stacks chain DNS → L4 → L7 → app (and how k8s Service vs Ingress maps to it), a decision checklist, and gotchas (client-IP loss and the PROXY protocol, timeout mismatches, 502s on deploy).
- [system-design/message-queue-vs-pubsub.md](system-design/message-queue-vs-pubsub.md) — message queues (one consumer per message) vs pub/sub (broadcast to all subscribers), where an event bus fits (usually pub/sub semantics plus content-based routing, in-process or distributed), and how Spring Boot's `ApplicationEventPublisher` (in-process pub/sub) compares to JMS (broker-backed queue or topic destinations).
- [system-design/monolith-vs-microservices.md](system-design/monolith-vs-microservices.md) — what "service" means (Order Service, Payment Service, etc.): independent processes/databases/deploy lifecycles talking over the network vs modules sharing one process in a monolith, plus a decision checklist for which to build.
- [system-design/mysql-table-vs-mongo-document.md](system-design/mysql-table-vs-mongo-document.md) — how a MySQL table differs from a MongoDB document, despite both being field→value stores.
- [system-design/rate-limiters.md](system-design/rate-limiters.md) — common rate limiting strategies (fixed window, sliding window log/counter, token bucket, leaky bucket) and Uber's distributed control-plane architecture (local probabilistic dropping instead of per-request Redis lookups).
- [system-design/redis-persistence-rdb-vs-aof.md](system-design/redis-persistence-rdb-vs-aof.md) — Redis persistence: RDB snapshots vs AOF write log, trade-offs and when to use each.
- [system-design/redis-single-threaded.md](system-design/redis-single-threaded.md) — how Redis works, why it's called single-threaded, why `INCR` is atomic, and use cases beyond caching.
- [system-design/rest-api-path-vs-query-params.md](system-design/rest-api-path-vs-query-params.md) — `/users/{id}` vs `/users?userId=xxx`: why path params are conventional for identifying one unique resource and query params for filtering/sorting/paging a collection, a postal-address analogy, and a decision checklist with GitHub/Stripe examples for each option.
- [system-design/reverse-proxy-protocol-termination.md](system-design/reverse-proxy-protocol-termination.md) — why the standard production shape is HTTP/2 (+TLS/HTTP/3) between browsers and the reverse proxy but plain HTTP/1.1 keep-alive between the proxy and the app: that a proxy is two independent connections (so each side's protocol is a separate choice), which HTTP/2 features only pay off on a high-RTT public network, why HTTP/2 to the backend can hot-spot one instance behind an L4 balancer, a shipping-port analogy, the gotchas (`X-Forwarded-*` and `server.forward-headers-strategy`, NGINX's HTTP/1.0-upstream default silently killing keep-alive, WebSocket/SSE, HTTP/3) with NGINX and Spring config, and a decision checklist for edge termination vs end-to-end HTTP/2 (gRPC, service mesh).
- [system-design/saga-pattern-compensating-transactions.md](system-design/saga-pattern-compensating-transactions.md) — the Saga pattern: handling a later step (e.g. payment) failing after an earlier step (e.g. order creation) already committed, via compensating transactions, orchestration vs choreography, how to decide between them, and idempotency.
- [system-design/scheduler-internals.md](system-design/scheduler-internals.md) — why schedulers don't poll every millisecond: the deadline-based sleep design (min-heap of next-fire-times, block until exactly the soonest one), timer wheels for millions of short-lived timers (Netty, Linux kernel, Kafka), hardware timer interrupts and tickless kernels, and how Spring's `@Scheduled` fits this model.
- [system-design/thundering-herd-problem.md](system-design/thundering-herd-problem.md) — the cache-stampede problem (many concurrent requests regenerating an expired shared resource at once and overwhelming the backend), a herd-stampede analogy, mitigation techniques (single-flight lock, stale-while-revalidate, jittered TTL, probabilistic early expiration, background refresh, client backoff+jitter) with a decision checklist for which fits a given system. Also disambiguates **thundering herd vs cache stampede vs celebrity/hot key** — the first two are temporal correlation (many actors at the same instant) while the third is distributional skew (one key busier than a node can serve), so TTL jitter and single-flight fix the former and do nothing for the latter while key salting does the reverse; why they compound (the celebrity key has the most concurrent readers, so it produces the biggest stampede, and a hot key usually needs both fixes); plus **cache avalanche** (many keys expiring together or a cache restart) and **cache penetration** (requests for keys that never exist) as two more in the family; and how to tell them apart from the metrics — stampede and avalanche are spikes, a celebrity is a plateau.
- [system-design/websockets.md](system-design/websockets.md) — why HTTP's request/response shape is backwards for push and what polling and long polling cost; the `Upgrade` handshake in detail (`Connection: Upgrade` + `Upgrade: websocket` → **`101 Switching Protocols`**, after which the connection is no longer HTTP at all), why `Sec-WebSocket-Accept` hashes a public GUID to prove the server understands the protocol rather than to authenticate anyone, and why client frames are masked to defend *broken proxies* rather than for confidentiality; frames and opcodes, and why ping/pong heartbeats are mandatory in practice because LB and NAT idle timeouts cull silent connections; the infrastructure consequences that make this a systems topic — a socket pins a client to one instance, so deploys disconnect everyone, autoscaling turns lumpy, **round robin balances connections rather than load**, the reverse proxy must be configured to pass upgrades, cross-instance delivery needs a pub/sub backplane, and capacity is sized by connection count rather than request rate (C10K, event loops); security including the fact that **the same-origin policy does not apply to WebSockets**, so an unvalidated `Origin` means Cross-Site WebSocket Hijacking; the alternatives table with the rule that one-way flows should use SSE; RFC 8441 for HTTP/2; a decision checklist with real-life examples per option; and common mistakes.
- [system-design/write-ahead-log.md](system-design/write-ahead-log.md) — why a database can't just write the row and ack (random I/O cost, non-atomic torn pages, and transactions spanning many pages), the WAL rule that a page may not reach the data files before its log record is durable and a commit *is* its durable commit record, why writing twice is faster than writing once badly (sequential vs random, one `fsync` per commit regardless of pages touched, and group commit amortising even that), `fsync` as the whole durability story and the stack-wide ways it's lost (page cache vs device, drives that lie about flush barriers, the Linux fsync-gate and why Postgres now panics), checkpointing as the dial between steady-state I/O and recovery time, ARIES crash recovery as analysis-REDO-UNDO with per-page LSNs making replay idempotent and per-record checksums making a torn log tail normal rather than an error, redo vs undo logs and why InnoDB has both a redo log and a separate binlog, what the log then gives away free (replication as permanent crash recovery, point-in-time recovery, CDC/logical decoding, standbys), a full mapping of Redis **AOF** onto WAL — that AOF is write-*behind* so `everysec` can lose already-acknowledged writes while `always` closes the gap, that it logs *commands* rather than page changes and so must rewrite non-deterministic ones (`SPOP`→`SREM`, relative→absolute expiry) exactly like statement-based replication, plus AOF-rewrite as checkpointing and hybrid AOF as base-backup-plus-log, a toy Kotlin WAL showing that the whole mechanism is the order of four lines, decision checklists for fsync policy / log-vs-snapshot / checkpoint frequency with real-life examples per option, and common mistakes.
- [system-design/zookeeper-distributed-coordination.md](system-design/zookeeper-distributed-coordination.md) — what ZooKeeper is (a small strongly-consistent replicated store for *coordination metadata*, not application data) and the problem it factors out: every distributed system needs agreement on a few small facts — who leads, who's alive, who holds the lock — and hand-rolling that consensus produces split brain, so it's solved once centrally; the znode data model and why **ephemeral + sequential** is the primitive behind elections, locks and membership (self-cleaning when a client dies); sessions and heartbeats as the actual liveness mechanism (your *session* being alive matters, not your process); watches as one-shot notifications that tell you *that* something changed, not what; the guarantee asymmetry that writes are linearizable through the leader via ZAB quorum while **reads are local and may be stale** (so more servers means faster reads and slower writes); the standard recipes with a Curator leader-election example, including watching your predecessor rather than the parent to avoid the herd effect; a full walk-through of **what happens during a partition** — majority quorum, why two disjoint groups can't both have one, why the minority side refuses writes and expires sessions, how an application learns which node to write to, and why losing your session means losing leadership immediately no matter how healthy you feel; **fencing tokens** and why a distributed lock alone is unsafe against a GC-paused holder; what ZooKeeper is *not* (database, queue, request-path dependency); where it's used and Kafka's move off it to KRaft; etcd and Consul as alternatives; decision checklists for whether you need a coordination service at all and which one; and common mistakes.

## To Be Revised

- [spring-boot/sql-injection-protection.md](spring-boot/sql-injection-protection.md)
